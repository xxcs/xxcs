import random

class EnhancedRandom()
def EnhancedRandom(start, end, enchane=dict()):
    length = end - start

    record = dict()
    for key, value in enchane.iteritems():
        if isinstance(key, (list, tuple)):

        elif isinstance(key, (dict,)):

        else:



class EnhancedRandom(object):
    def